class Line {
	field Square square;
	
	constructor Line new(int top, int size, int left, int right){
		while (left < right){
			let square = Square.new(left,top,size);
			do square.draw();
			let left = left + 1;
		}
		
		return this;
	}
}